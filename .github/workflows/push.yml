on:
  push:
    branches:
      - main
      - publish_docker

jobs:
  test:
    name: Build And Test
    uses: ./.github/workflows/build.yml
    secrets: inherit

  publish:
    name: Publish Registration Page
    uses: ./.github/workflows/registration-docker.yml
    secrets: inherit